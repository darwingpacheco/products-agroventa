apiVersion: v1
kind: ConfigMap
metadata:
  name: products-config
data:
  PORT: "8080"
  JWT_EXPIRATION_SECONDS: "3600"
  JWT_ISSUER: "agroventa-products-service"
  DB_POOL_MAX_SIZE: "20"
  R2DBC_URL: "r2dbc:postgresql://REPLACE_RDS_HOST:5432/authdb"
  FLYWAY_JDBC_URL: "jdbc:postgresql://REPLACE_RDS_HOST:5432/authdb"
